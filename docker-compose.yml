version: '3.8'
services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    env_file:
      - .env  # Carga automáticamente el archivo .env
    environment:
      - NODE_ENV=production
    command: ["node", "dist/index.js"]
